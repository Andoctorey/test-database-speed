apply plugin: 'java'

dependencies {
    compile('de.greenrobot:DaoGenerator:1.+')
}

sourceSets {
    main {
        java {
            srcDir 'src'
            srcDir 'src-gen'
        }
    }
}

artifacts {
    archives jar
}